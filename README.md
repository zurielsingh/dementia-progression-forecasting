# dementia-progression-forecasting-
Next-visit MMSE prediction using gated recurrent neural networks on the OASIS-2 longitudinal dataset — BiLSTM achieves R²=0.75 across a 108-config hyperparameter sweep with strict patient-wise leakage prevention.
